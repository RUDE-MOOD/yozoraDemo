---
description: Run the UI/UX Pro Max design agent
---
1. Analyze the user's request to understand their design needs (product type, style, etc.).
2. Read the `skills/ui-ux-pro-max/SKILL.md` file to understand the usage.
3. Use the `ui-ux-pro-max` skill to generate a design system or provide design recommendations.
   - If starting a new design, run the design system search.
   - If refining, run specific domain searches.
